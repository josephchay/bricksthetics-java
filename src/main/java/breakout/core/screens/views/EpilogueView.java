package breakout.core.screens.views;

import breakout.helpers.AutomatedLog;
import javafx.stage.Stage;

/**
 * The static UI view for the end screen
 */
public class EpilogueView extends View{
    @Override
    public void build(Stage stage) {
        AutomatedLog.wrapSys("Building view template for epilogue", () -> {

        });
    }
}
